name: Build Windows EXE

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      shell: cmd
      run: |
        python -m pip install --upgrade pip
        pip install pandas openpyxl Flask Werkzeug pyinstaller

    - name: Build EXE (directory mode)
      shell: cmd
      run: |
        pyinstaller --clean --noconfirm pyinstaller.spec

    - name: Check output
      shell: pwsh
      run: |
        Write-Host "Contents of dist folder:"
        Get-ChildItem dist/ -ErrorAction SilentlyContinue -Recurse

    - name: Create zip archive
      shell: pwsh
      run: |
        Compress-Archive -Path "dist/Excel数据脱敏工具" -DestinationPath "dist/Excel数据脱敏工具-Windows.zip" -Force

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: Excel数据脱敏工具-Windows
        path: dist/Excel数据脱敏工具-Windows.zip
