name: Build Windows EXE

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      shell: cmd
      run: |
        python -m pip install --upgrade pip
        pip install pandas openpyxl Flask Werkzeug pyinstaller

    - name: Build EXE
      shell: cmd
      run: |
        pyinstaller --clean --noconfirm --name "Excel数据脱敏工具" --windowed --add-data "templates;templates" --add-data "static;static" --add-data "desensitizer.py;." --hidden-import pandas --hidden-import openpyxl --hidden-import flask --hidden-import werkzeug --hidden-import jinja2 --hidden-import numpy app.py

    - name: Check output
      shell: pwsh
      run: |
        Write-Host "Contents of dist folder:"
        Get-ChildItem dist/ -ErrorAction SilentlyContinue

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: Excel数据脱敏工具-Windows
        path: dist/*.exe
